---
name: login
description: Log in to Yutori to connect your account
argument-hint: ""
---

# Yutori Login

Run this command in your terminal:

```bash
yutori login
```

A browser window will open. Complete sign-in, then return to terminal.

If you haven't installed the CLI yet:

```bash
pip install yutori-mcp
```
